// Default local configuration for E-Ink Weather Dashboard
// These will be overridden at run-time when running inside Docker
window.EINK_ENV = {
  THEME: "light",
  SCENARIO: "springShowers",
  SHOW_CONTROLS: "true"
};
